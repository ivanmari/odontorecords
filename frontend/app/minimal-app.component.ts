import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <h1>OdontoRecords (minimal)</h1>
    <p>Minimal app running to verify Angular CLI setup.</p>
  `
})
export class MinimalAppComponent {}
